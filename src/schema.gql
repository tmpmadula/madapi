# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Notification implements BaseNotification {
  targetId: String!
  userId: String!
  content: String!
  id: ID!
  status: NotificationStatus!
  createAt: String!
}

interface BaseNotification {
  targetId: String!
  userId: String!
  content: String!
}

enum NotificationStatus {
  Active
  Deleted
}

type Article implements BaseArticle {
  articleId: String!
  userId: String!
  slug: String!
  title: String!
  description: String!
  body: String!
  id: ID!
  status: articleStatus!
  createAt: String!
}

interface BaseArticle {
  articleId: String!
  userId: String!
  slug: String!
  title: String!
  description: String!
  body: String!
}

enum articleStatus {
  Active
  Deleted
}

type Query {
  notification(id: ID!): Notification!
  notificationByUserId(userId: String!): [Notification!]!
  notificationByTargetId(targetId: String!): [Notification!]!
  getArticle(id: ID!): Article!
  articleByUserId(userId: String!): [Article!]!
}

type Mutation {
  createNotification(input: CreateNotificationInput!): Notification!
  updateNotification(input: UpdateNotificationInput!, id: ID!): Notification!
  createarticle(input: CreateArticleInput!): Article!
  updatearticle(input: UpdateArticleInput!, id: ID!): Article!
}

input CreateNotificationInput {
  targetId: String!
  userId: String!
  content: String!
}

input UpdateNotificationInput {
  status: NotificationStatus!
}

input CreateArticleInput {
  articleId: String!
  userId: String!
  slug: String!
  title: String!
  description: String!
  body: String!
}

input UpdateArticleInput {
  slug: String!
  title: String!
  description: String!
  body: String!
  tags: String!
  id: String!
}
